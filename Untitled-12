composer global require drush-ops/drush    drush/drush
drush-ops/drush

chmod +x drush.phar
export PATH="$HOME/.composer/vendor/bin:$PATH"




cd html
composer require drush/drush:master
vendor/bin/drush si standard --db-url=mysql://drupal_usr:qwerty12q@db_mysql/drupal_8
vendor/bin/drush -y si standard --db-url=mysql://${MYSQL_USER}:${MYSQL_PASSWORD}@${MYSQL_HOST}/${MYSQL_DATABASE} --site-name=Example --account-name=admin --account-pass=admin